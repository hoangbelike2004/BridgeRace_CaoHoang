using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEditor.SceneManagement;
using UnityEngine;

public class Stait : CharacterBrick
{
    public GameObject wallStait;
    //private void OnTriggerEnter(Collider other)
    //{
    //    if (colorType == other.gameObject.GetComponent<Character>().colorType)
    //    {
    //        wallStait.SetActive(false);
    //    }
    //    else if (colorType != other.gameObject.GetComponent<Character>().colorType)//!= colorType
    //    {
    //        wallStait.SetActive(true);
    //        // other.gameObject.GetComponent<Stait>().wallStait.SetActive(true);
    //        if (other.gameObject.GetComponent<Character>().valuesBrick > 0)//when remove brick
    //        {
    //            wallStait.SetActive(false);

    //        }
            
    //    }
    //}
}
